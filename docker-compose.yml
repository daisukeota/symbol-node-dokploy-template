services:
  # 1. 初期化タスク
  initializer:
    build: ./initializer
    volumes:
      - symbol_startup:/app/dest_startup
      - symbol_userconfig:/app/dest_userconfig
      - symbol_mongo:/app/dest_mongo
      - symbol_seed:/app/dest_seed
      - symbol_certificates:/app/dest_certificates
    environment:
      - SYMBOL_NETWORK=${SYMBOL_NETWORK:-mainnet}
      - NODE_NAME=${NODE_NAME:-MyDokployNode}
      - DOMAIN_NAME=${DOMAIN_NAME}
      - MAIN_PRIVATE_KEY=${MAIN_PRIVATE_KEY}
      - VRF_PRIVATE_KEY=${VRF_PRIVATE_KEY}
      - REMOTE_PRIVATE_KEY=${REMOTE_PRIVATE_KEY}
      - VOTING_PRIVATE_KEY=${VOTING_PRIVATE_KEY}
    networks:
      - dokploy-network

  # 2. データベース本体（全てのネットワークと外部公開ポートの親玉になります）
  db:
    image: mongo:7.0.23
    hostname: db
    command: mongod --replSet rs0 --bind_ip_all
    volumes:
      - mongodb_data:/data/db
    ports:
      - "3000:3000"  # REST API 用のポートをここで一括公開
      - "7900:7900"  # Node 用のポートをここで一括公開
    restart: always
    networks:
      - dokploy-network
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.runCommand({ping:1})", "--quiet"]
      interval: 3s
      timeout: 3s
      retries: 5

  # 3. MongoDB レプリカセット初期化タスク
  mongo-init:
    image: mongo:7.0.23
    user: root
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - symbol_startup:/startup:ro
    network_mode: "service:db"
    command:
      - /bin/bash
      - -c
      - |
        echo "Waiting for db connection..."
        until mongosh --eval "db.runCommand({ping:1})" --quiet; do
          sleep 1
        done
        
        echo "Initializing replica set..."
        mongosh --eval "
          try {
            rs.initiate({ _id: 'rs0', members: [{ _id: 0, host: 'db:27017' }] });
          } catch (e) {
            print('Replica set already initialized or skipped: ' + e.message);
          }
        " --quiet || true

        echo "Waiting for MongoDB to elect a PRIMARY node..."
        until mongosh --eval "db.hello().isWritablePrimary" --quiet | grep -q "true"; do
          sleep 1
        done
        echo "MongoDB is now PRIMARY and ready for writes!"
        exit 0

  # 4. Symbol Broker
  broker:
    image: symbolplatform/symbol-server:gcc-1.0.3.9
    user: root
    depends_on:
      initializer:
        condition: service_completed_successfully
      mongo-init:
        condition: service_completed_successfully
    volumes:
      - symbol_startup:/startup:ro
      - symbol_userconfig:/userconfig:ro
      - symbol_seed:/seed:ro
      - symbol_certificates:/certificates:ro
      - symbol_data_broker:/data
    network_mode: "service:db"
    # 👇 起動直前に自分自身のhostsに db を登録するトリックを仕込みます
    command: /bin/bash -c "echo '127.0.0.1 db' >> /etc/hosts && umask 077 && mkdir -p /data/startup && touch /data/startup/datadir-initialized && rm -f /data/*.lock && exec /bin/bash /startup/startBroker.sh /userconfig"
    restart: always
    stop_signal: SIGINT

  # 5. Symbol ノード本体 (Peer)
  node:
    image: symbolplatform/symbol-server:gcc-1.0.3.9
    user: root
    depends_on:
      initializer:
        condition: service_completed_successfully
      broker:
        condition: service_started
    volumes:
      - symbol_startup:/startup:ro
      - symbol_userconfig:/userconfig:ro
      - symbol_seed:/seed:ro
      - symbol_certificates:/certificates:ro
      - symbol_data_node:/data
    network_mode: "service:db"
    # 👇 起動直前に自分自身のhostsに db を登録するトリックを仕込みます
    command: /bin/bash -c "echo '127.0.0.1 db' >> /etc/hosts && umask 077 && mkdir -p /data/startup && touch /data/startup/mongo-initialized && rm -f /data/*.lock && exec /bin/bash /startup/startServer.sh /userconfig"
    restart: always
    stop_signal: SIGINT

# 6. REST ゲートウェイ（API）
  rest-gateway:
    image: symbolplatform/symbol-rest:2.5.1
    user: root
    depends_on:
      initializer:
        condition: service_completed_successfully
      node:
        condition: service_started
    volumes:
      - symbol_startup:/startup:ro
      - symbol_userconfig:/userconfig:ro
      - symbol_certificates:/certificates:ro # 👈 【超重要】これをつなぎ込みます！
    network_mode: "service:db"
    command: npm start --prefix /app /userconfig/rest.json
    restart: always

networks:
  dokploy-network:
    external: true

volumes:
  symbol_startup:
  symbol_userconfig:
  symbol_mongo:
  symbol_seed:
  symbol_certificates:
  symbol_data_broker:
  symbol_data_node:
  mongodb_data: