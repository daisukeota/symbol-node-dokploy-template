services:
  # 1. 初期化タスク（Shoestringによる鍵・設定ファイル生成）
  initializer:
    build: ./initializer
    volumes:
      - symbol_startup:/app/dest_startup
      - symbol_userconfig:/app/dest_userconfig
      - symbol_mongo:/app/dest_mongo
      - symbol_seed:/app/dest_seed
      - symbol_certificates:/app/dest_certificates
    environment:
      - SYMBOL_NETWORK=${SYMBOL_NETWORK:-mainnet}
      - NODE_NAME=${NODE_NAME:-MyDokployNode}
      - DOMAIN_NAME=${DOMAIN_NAME}
      - MAIN_PRIVATE_KEY=${MAIN_PRIVATE_KEY}
      - VRF_PRIVATE_KEY=${VRF_PRIVATE_KEY}
      - REMOTE_PRIVATE_KEY=${REMOTE_PRIVATE_KEY}
      - VOTING_PRIVATE_KEY=${VOTING_PRIVATE_KEY}
    networks:
      - dokploy-network

  # 2. データベース本体
  mongo:
    image: mongo:7.0.23
    command: mongod --replSet rs0 --bind_ip_all
    volumes:
      - mongodb_data:/data/db
    restart: always
    networks:
      - dokploy-network
    # mongo単体での生存確認ヘルスチェックを定義
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.runCommand({ping:1})", "--quiet"]
      interval: 3s
      timeout: 3s
      retries: 5

  # 3. MongoDB レプリカセット初期化タスク
  mongo-init:
    image: mongo:7.0.23
    user: root
    depends_on:
      mongo:
        condition: service_healthy # mongoがReadyになるまで絶対に動かない
    volumes:
      - symbol_startup:/startup:ro
    network_mode: "service:mongo"
    command:
      - /bin/bash
      - -ec
      - |
        mongosh --eval "
          try {
            rs.status();
          } catch (e) {
            rs.initiate({ _id: 'rs0', members: [{ _id: 0, host: 'mongo:27017' }] });
          }
        " --quiet

  # 4. Symbol Broker
  broker:
    image: symbolplatform/symbol-server:gcc-1.0.3.9
    user: root
    depends_on:
      initializer:
        condition: service_completed_successfully # initializerが「正常終了」するまで絶対にマウントさせない
      mongo-init:
        condition: service_completed_successfully # DB初期化が終わるまで待つ
    volumes:
      - symbol_startup:/startup:ro
      - symbol_userconfig:/userconfig:ro
      - symbol_seed:/seed:ro
      - symbol_certificates:/certificates:ro
      - symbol_data_broker:/data
    command: /bin/bash -c "rm -f /data/*.lock && exec /bin/bash /startup/startBroker.sh /userconfig"
    restart: always
    stop_signal: SIGINT
    networks:
      - dokploy-network

  # 5. Symbol ノード本体 (Peer)
  node:
    image: symbolplatform/symbol-server:gcc-1.0.3.9
    user: root
    depends_on:
      initializer:
        condition: service_completed_successfully # 同上
      broker:
        condition: service_started
    volumes:
      - symbol_startup:/startup:ro
      - symbol_userconfig:/userconfig:ro
      - symbol_seed:/seed:ro
      - symbol_certificates:/certificates:ro
      - symbol_data_node:/data
    ports:
      - "7900:7900"
    command: /bin/bash -c "rm -f /data/*.lock && exec /bin/bash /startup/startServer.sh /userconfig"
    restart: always
    stop_signal: SIGINT
    networks:
      - dokploy-network

  # 6. REST ゲートウェイ（API）
  rest-gateway:
    image: symbolplatform/symbol-rest:2.5.1
    user: root
    depends_on:
      initializer:
        condition: service_completed_successfully # これによりrest.jsonがフォルダになるバグを100%防ぐ
      node:
        condition: service_started
    volumes:
      - symbol_startup:/startup:ro
      - symbol_userconfig:/userconfig:ro
    ports:
      - "3000:3000"
    # 先ほど特定した正しいパスと、実行ディレクトリの移動を美しく内包
    command: /bin/bash -c "cd /app/src && node index.js"
    restart: always
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true

volumes:
  symbol_startup:
  symbol_userconfig:
  symbol_mongo:
  symbol_seed:
  symbol_certificates:
  symbol_data_broker:
  symbol_data_node:
  mongodb_data: